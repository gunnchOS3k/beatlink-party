# Runtime smoke notes — Beat Link

- Automated logic: `pnpm vitest run tests/gate1_core_loop.test.ts` (RoomManager full loop).
- Uses local `content/songs/approved-demo-catalog.json` only (`demo_generated` license).
- Does not rip/download copyrighted audio.
- Optional: `pnpm dev` then manual device runbook for PHYSICAL_PLAYTEST.
